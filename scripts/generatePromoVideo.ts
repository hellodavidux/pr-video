/**
 * Promo video pipeline entry point.
 *
 * This project runs the pipeline as ESM JavaScript:
 *   node scripts/generatePromoVideo.mjs [--json pr.example.json | --pr-url <url>]
 *
 * See scripts/generatePromoVideo.mjs for the full implementation.
 */
export {}
